[package]
name = "spm-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Core library for spm: config parsing, planning, shared types"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "2"
shellexpand = "3"
